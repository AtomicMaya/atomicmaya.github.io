---
interface Props {
    text: string;
    url: string;
}

const { text, url } = Astro.props;
---

<a class="content-link" href={url} target="_blank" rel="noopener"
    ><em class="fab fa-twitter-square"></em><span>{text}</span></a
>
